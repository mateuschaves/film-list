import { StyleSheet } from 'react-native';

const styles =  StyleSheet.create({
    container: {
        flex: 1,
        padding: 16,
        paddingTop: 80,
    },
    title: {
        fontSize: 20,
        fontWeight: "300",
        color: "#E32929",
        textAlign: "left",
        marginTop: 60,
        marginBottom: 20
    }
})

export default styles;